version: "3.9"
services:
    app:
        build:
            context: ./app
            dockerfile: Dockerfile
        volumes:
            - ./db:/usr/db
        working_dir: /usr/app
        command: "poetry run gunicorn financez.wsgi"
        command: [nginx, '-g', 'daemon off;']
        ports:
            - 80:80
